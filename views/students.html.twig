<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <title>Tasks</title>
  </head>
  <body>
    <div class='container'>
      {% if results is not empty %}
        <h3>Your tasks</h3>
        {% for i in 0..results|length - 1 %}
          <ul>
            <li>
              {{ results[i].getDescription }} in 
              {{ results_categories[i]|length > 1 ? 'categories' : 'category' }}
              {% for j in 0..results_categories[i]|length - 1 %}
                {% if j > 0 %}
                  and
                {% endif %}
                {{ results_categories[i][j].getName }}
              {% endfor %}
              is due on {{ results[i].getDueDate }}
            </li>
          </ul>
        {% endfor %}
      {% else %}
        <h3>You have no tasks fool!</h3>
      {% endif %}

      <a href='/'>Home!</a>
    </div>
  </body>
</html>
